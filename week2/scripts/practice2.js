function select(obj){
    // 탭을 클릭했을 때
    // 1. 선택된 요소의 하위에 있는 nav-link에게만 active클래스를 추가해주어 선택처리
    // 2. 선택된 요소의 data-nav-eq와 같은 값(data-box-eq)을 갖는 jumbtron의 hide 클래스를 삭제하여 보이기 처리 (다른 요소는 보이면안됨)
    
    // jQuery 혹은 순수 자바스크립트로 작업하시면 됩니다.

    // jQuery에서 attribute로 검색하기 위해서는 
    // $('클래스명[속성_이름=속성_값]')
    // 위와 같이 검색합니다.
}